{% macro menu_item(admin) %}
    <li{% if is_active_path(admin.defaultUrl) %} class="active"{% endif %}>
        <a href="{{ admin.defaultUrl }}" >{{ admin.option('label')|transchoice(10) }}</a>
    </li>
{% endmacro %}